export interface NotificationIn {
    transfer_id: string,
    user_id: string,
    text: string,
}

export interface NotificationOut {
    transfer_id: string,
    user_id: string,
    text: string,
}