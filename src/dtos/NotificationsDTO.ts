export interface NotificationIn {
    transfer_id: string,
    account_id: string,
    text: string,
}

export interface NotificationOut {
    transfer_id: string,
    account_id: string,
    text: string,
}